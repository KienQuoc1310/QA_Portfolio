# User API Testing

## 1. Overview

This document defines the test coverage for User API endpoints.

The User API is responsible for managing user resources.

The main operations are:

```text
Create User
    ↓
Get User
    ↓
Update User
    ↓
Delete User
